import React from "react";
import { Text } from "react-native";

const RepoCard = ({repo}) =>{
    console.log(repo)
    return <></>
}

const ReposList = ( {repos} ) =>
{
    console.log(repos);
    return <Text>R</Text>;
}

export default ReposList;